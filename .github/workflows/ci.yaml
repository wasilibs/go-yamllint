name: CI
on:
  push:
    branches:
      - main
    tags:
      - "*"
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: wasilibs/actions/.github/workflows/ci.yaml@435fbabb9723e77fd6ba71e6ec8744efd1a56be1
    secrets: inherit
